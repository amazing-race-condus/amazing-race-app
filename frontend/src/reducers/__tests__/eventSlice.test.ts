import eventReducer, { setEvent } from "../eventSlice"
import type { Event } from "@/types"

describe("eventSlice reducers", () => {
//   const initialState: Notification = {
//     message: "",
//     type: null
//   }

  test("should set event", () => {
    expect(9).toBe(9)
  })

})
generator client {
  provider = "prisma-client-js"
  output   = "./prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Checkpoint {
  id   Int     @id @default(autoincrement())
  name String?
}
